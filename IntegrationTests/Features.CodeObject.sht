a = <!
          c++; 
          b+=3; 

          //ScriptProg has read access to 
          //global variables 
          Test.AreEqual(3, c);
          Test.AreEqual(8, b);
          
          //in order to modify them it should
          //use global

          global:c+=10;

          return 2;!>;

c=2;
b=5;

f = a();

Test.AreEqual(2, f);
Test.AreEqual(12, c);
Test.AreEqual(5, b);

Test.AreEqual(2,  eval('1+1'));


a = <! return a+b; !>;

Test.AreEqual(19, a([a->9, b->10]));
